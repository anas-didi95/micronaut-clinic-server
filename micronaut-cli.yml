applicationType: default
defaultPackage: com.anasdidi.clinic
testFramework: junit
sourceLanguage: java
buildTool: maven
features:
  [
    annotation-api,
    app-name,
    data,
    data-jdbc,
    http-client,
    jackson-databind,
    java,
    java-application,
    jdbc-hikari,
    junit,
    liquibase,
    logback,
    maven,
    netty-server,
    postgres,
    readme,
    shade,
    testcontainers,
    yaml,
  ]
